# TypeScript Rules

The codebase MUST:

- Use strict TypeScript
- Avoid `any`
- Prefer inferred types when clear
- Use explicit types for public APIs
- Share reusable domain types
- Prefer type-safe utilities
